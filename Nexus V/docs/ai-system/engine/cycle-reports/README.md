# Cycle Reports

`Invoke-AIOperationCycle.ps1` writes the latest operation-cycle report here.

Reports capture routine order, step status, duration, and failure messages so future AIs can continue from facts rather than rediscovery.

