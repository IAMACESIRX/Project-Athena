# Hypergraph Relational Processing

Hypergraph processing models relationships involving more than two nodes.

This is useful because project effects often involve several things at once: user direction, repo layer, file changes, runtime state, and tool outputs.
