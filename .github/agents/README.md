# GitHub Custom Agents

These files configure task-specific GitHub Copilot cloud agent profiles for the repository Agents tab.

Athena's internal agent registry remains in `agents/agent-registry.yml`. Keep the two surfaces aligned by intent, but do not duplicate private local runtime details here.
