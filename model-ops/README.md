# Model Operations

Model operations tracks the models, providers, routing rules, evaluation expectations, and safety limits used by the AI-OS control plane.

This folder is a contract for future runtime build-out. It does not require any cloud provider by default.

